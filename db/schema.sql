Write SQL queries here that do the following:

Create the burgers_db

Switch to or use burgers_db
Create a burgers table with these fields:
    id: au tuto incrementing int that serves as the primary key
    burger_name: a string
    devoured: a boolean

Run the schema sql and seeds sql files into the mysql server from the command line.

Run the SQL files.

Now, run these SQL files:
     Make sure you are in teh db filder of your app.
    Start MySQL command line tool and login: mysql -u root -p.
    (etc. read directions in GitLab )