Import(require) RTCPeerConnection.js into onpointermove.js;

In the orm.js File, create the methods that will execute the necessary SQL commands in the controllers.These are the methods you will need to use in order to retrieve and store data in your database.
selectAll();
insertOne();
updateOne();

Export the ORM object in module.exports.